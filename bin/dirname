#!/bin/sh

# from https://github.com/dylanaraps/pure-sh-bible

if [ -z "$1" ]; then
	return 1
fi

dir="$1"
dir=${dir%%"${dir##*[!/]}"}

if [ "${dir##*/*}" ]; then
	dir=.
fi

dir=${dir%/*}
dir=${dir%%"${dir##*[!/]}"}

echo "${dir:-/}"
